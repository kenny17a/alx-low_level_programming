#include <stdio.h>

/**
 * main - Entry point
 *
 * @argc: gives the size of the array elements
 *
 * @argv: gives the elements in the array
 *
 * Return: (0) on success.
 */
int main(int argc __attribute__((unused)), char *argv[])
{

	puts(argv[0]);

	return (0);

}
